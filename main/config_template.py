path = "Desktop/forkbomb/" ##Trailing slash is important.
DEBUG = True
DO_LOGGING = True # Do we log all the debug prints or not?

# # TODO - unused?
# SENDFILE_BACKEND = 'sendfile.backends.development' # For local copies, use this. For apache, sendfile.backends.xsendfile. For nginx, sendfile.backends.nginx.
# # Absolute filesystem path to the directory that will hold anything sent via sendfile when using nginx.
# SENDFILE_ROOT = None
# # For nginx sendfil, this has to match a thing in /etc/nginx/nginx.conf or something.  https://pypi.python.org/pypi/django-sendfile
# SENDFILE_URL = None